[tool.poetry]
name = "dockerfiles"
version = "0.1.0"
description = ""
authors = ["Judah Schwartz <juschwartz@paloaltonetworks.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
opencv-python = "==4.7.0.72"  # using previous version until fixed https://github.com/opencv/opencv-python/issues/676
scikit-learn = "==1.2.2"
pandas = "==1.5.3"
beautifulsoup4 = "*"
dill = "==0.3.6"
catboost = "==1.1.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
