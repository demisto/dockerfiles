[tool.poetry]
name = "dockerfiles"
version = "0.1.0"
description = ""
authors = ["Judah Schwartz <juschwartz@paloaltonetworks.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "~3.9"
opencv-python = "==4.5.5.64"  # using previous version until fixed https://github.com/opencv/opencv-python/issues/676
scikit-learn = "==1.0.2"   # cannot upgrade, the newer versions are not BC with the training model. 
pandas = "*"
beautifulsoup4 = "*"
dill = "==0.3.4"
catboost = "==1.2.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
