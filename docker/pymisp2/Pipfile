[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
pymisp = {version = ">=2.4.89,<=2.4.135.3", extras=["email"]}
# The earliest version of emailObject and latest version before pymisp started using extract_msg

[dev-packages]

[requires]
python_version = "3.8"
