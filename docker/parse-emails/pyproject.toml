[tool.poetry]
name = "dockerfiles"
version = "0.1.0"
description = ""
authors = ["Your Name <you@example.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "~3.12"
parse-emails = {git = "https://github.com/demisto/parse-emails.git", rev = "XSUP-53018-fix-mime-entity-with-crlf-parsing"}

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
