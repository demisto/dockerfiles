# Minimal throwaway image used to exercise the Sigstore/cosign signing flow
# end-to-end (CIAC-16370). It only adds a label on top of an existing published
# base so the build is fast and has no external build dependencies.
FROM demisto/python3:3.12.14.12343672

LABEL com.demisto.cosign-test="CIAC-16370"
