variables:
  CURRENT_BRANCH_NAME: remove_clone_private_from_dockerfiles_pipelines

include:
  - file: "/.gitlab/ci/dockerfiles/.gitlab-ci.yml"
    ref: remove_clone_private_from_dockerfiles_pipelines
    project: "${CI_PROJECT_NAMESPACE}/infra"
