variables:
  CURRENT_BRANCH_NAME: "update-dockerfiles-gitlab-image"

include:
  - file: "/.gitlab/ci/dockerfiles/.gitlab-ci.yml"
    ref: "update-dockerfiles-gitlab-image"
    project: "${CI_PROJECT_NAMESPACE}/infra"
