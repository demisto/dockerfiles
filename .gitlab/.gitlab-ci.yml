variables:
  CURRENT_BRANCH_NAME: infra-dockerfiles # FIXME! change before merging to $INFRA_BRANCH

include:
  - file: "/.gitlab/ci/dockerfiles/.gitlab-ci.yml"
    ref: infra-dockerfiles # FIXME! change before merging to $INFRA_BRANCH
    project: "${CI_PROJECT_NAMESPACE}/infra"
